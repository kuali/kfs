<!DOCTYPE doc>
<Context path="/${ant.project.name}-${build.environment}" reloadable="false" ${context.docbase}>
    <Manager className="org.apache.catalina.session.StandardManager" maxActiveSessions="250" />
</Context>