# 4. RELEASE PACKAGING PROPERTIES

release=5-0
release.version.number=5.0
rice.release=2.1.1

release.tag.name=release-${release}
release.zip.name=../kfs-${release.version.number}.zip

dont.filter.project.schema=true

demo.database.cleanup.script=${database.directory}/kfs-demo.sql
bootstrap.database.cleanup.script=${database.directory}/kfs-bootstrap.sql
