# TEMPORARILY CUSTOMIZE A GIVEN EXECUTION OF THE BUILD

# this property determines whether the build will do a local deployment or package for a deployment on the test node
# set to false if you want to run the test deployment process locally to evaluate the output
is.local.build=true

# these properties are used for bypassing sections of clean-project and filter-project targets.
# only leave these sections uncommented if your help and rice information are infrequently updated,
# as the time saved by using these options is negligable unless builds are done frequently.
#no.filter.project.help
#no.filter.project.rice
#no.filter.project.spring.ide

# this property can be used to turn p6spy logging on and off (we use p6spy to log the sql generated by ojb)
use.p6spy=false
# this property can be used to turn the batch schedule on and off
use.quartz.scheduling=false

# this property determines what tests are included when the test target is executed via test-local
tests.includes=**/*Test.java

# this property determines what log statements will be printed by default and whether the logs go to the log file or standard out
appenders=INFO, LogFile, StdOut

# this is the kuali and workflow environment
build.environment=dev

# these properties are related to ${build.environment} - you should review them, if you change that property
# you will need to obtain the values for these properties for a given ${build.environment} from someone on the project
datasource.username=
datasource.password=
mysql.datasource.url=jdbc:mysql://esdbk01.uits.indiana.edu:3306/${datasource.username}
oracle.datasource.url=jdbc:oracle:thin:@esdbk02.uits.indiana.edu:1521:KUALI
# valid values are Oracle9i and MySQL
datasource.ojb.platform=Oracle9i

# determines the mail relay server used by MailService
mail.relay.server=
# from email address for mail sent by the batch framework (JobListener and other) and to email address for job failure messages
# this property determines where exception e-mails are sent when batch jobs fail - fill in your e-mail address
batch.mailing.list=
# to/from email address for mail sent by the incident reporting framework (KualiExceptionIncident, etc.)
# this property determined where exception e-mails are sent when the OLTP incident report is used - fill in your email address
incident.mailing.list=

# PERMANENTLY CUSTOMIZE THE BUILD FOR RICE DEVELOPMENT

# these properties represent the passwords for the rice databases - ask if you don't know what they should be.
# you do not need to fill these in, unless you will be doing rice development
rice.datasource.url=jdbc:oracle:thin:@bl-uits-beatles.ads.iu.edu:1521:SIT1INT
rice.datasource.username.dev=RICE092DEV
rice.datasource.password.dev=
rice.datasource.username.test=${rice.datasource.username.dev}
rice.datasource.password.test=${rice.datasource.password.dev}

# the institution specific files that define the spring context for the application 
# institution.spring.source.files=

# the institution specific files that are included in the application spring context for unit testing
# institution.spring.test.files=

# the institution specific configuration property files that should be available in the KualiConfigurationService context, along with configuration.properties
# institution.property.files=

# the institution specific configuration property files that should be added to the KualiConfigurationService context for unit testing
# institution.property.test.files=

# the institution specific files that define the struts context for the application
# institution.struts.config.files=

# the institution specific files containing user messages that will be displayed via struts tags in jsps
# institution.struts.message.resources=

# the institution specific classes that wrap java constants for jsps and tags (see ApplicationInitializeListener)
# institution.jstl.constants.classnames=

# the institution specific javascript files that should be available to every page in the application
# institution.javascript.files=

# the institution specific css files that should be available to every page in the application
# institution.css.files=
